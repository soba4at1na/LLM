services:
  db:
    image: postgres:15-alpine
    container_name: llm_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:?DB_USER_is_required}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD_is_required}
      POSTGRES_DB: ${DB_NAME:?DB_NAME_is_required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:?DB_USER_is_required} -d ${DB_NAME:?DB_NAME_is_required}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - llm_network

  backend:
    build: ./backend
    container_name: llm_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://${DB_USER:?DB_USER_is_required}:${DB_PASSWORD:?DB_PASSWORD_is_required}@db:5432/${DB_NAME:?DB_NAME_is_required}
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY_is_required}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      MODEL_PATH: ${MODEL_PATH:-}
      MODEL_DIR: ${MODEL_DIR:-/app/models}
      MODEL_AUTO_SELECT: ${MODEL_AUTO_SELECT:-true}
      MODEL_N_CTX: ${MODEL_N_CTX:-3072}
      MODEL_MAX_TOKENS: ${MODEL_MAX_TOKENS:-512}
      MODEL_N_THREADS: ${MODEL_N_THREADS:-10}
      MODEL_N_THREADS_BATCH: ${MODEL_N_THREADS_BATCH:-10}
      MODEL_N_BATCH: ${MODEL_N_BATCH:-256}
      MODEL_USE_MLOCK: ${MODEL_USE_MLOCK:-false}
      MODEL_USE_MMAP: ${MODEL_USE_MMAP:-true}
      LLM_REQUEST_TIMEOUT_SECONDS: ${LLM_REQUEST_TIMEOUT_SECONDS:-240}
      DISABLE_LLM: ${DISABLE_LLM:-false}
      CORS_ORIGINS: '["http://localhost:3000","http://127.0.0.1:3000"]'
    volumes:
      - ./backend/app:/app/app:ro
      - ./backend/models:/app/models:ro
    depends_on:
      db:
        condition: service_healthy
    networks:
      - llm_network

  frontend:
    image: nginx:alpine
    container_name: llm_frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    volumes:
      - ./frontend/index.html:/usr/share/nginx/html/index.html:ro
      - ./frontend/css:/usr/share/nginx/html/css:ro
      - ./frontend/js:/usr/share/nginx/html/js:ro
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    networks:
      - llm_network

volumes:
  postgres_data:

networks:
  llm_network:
    driver: bridge
